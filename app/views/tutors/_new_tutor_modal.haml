#new-tutor-modal.modal.fade{:role => "dialog", :tabindex => "-1"}
  .modal-dialog{:role => "document"}
    .modal-content
      .modal-header
        %button.close{"aria-label" => "Close", "data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
        %h4.modal-title New Tutor
      = simple_form_for(@tutor, :remote => true) do |f|
        .modal-body
          = f.error_notification
          = f.input :first_name
          = f.input :last_name
          = f.association :course_year
        .modal-footer
          %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
          = f.button :submit, class: 'btn-primary add-tutor'