= simple_form_for(@review) do |f|
  = f.error_notification

  .form-inputs
    = f.association :tutor
    = f.association :student
    = f.association :project

  .form-actions
    = f.button :submit
